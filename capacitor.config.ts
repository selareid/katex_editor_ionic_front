import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_katex_front',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
